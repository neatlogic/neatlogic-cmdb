<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright(c) 2021 TechSure Co., Ltd. All Rights Reserved.
  ~ 本内容仅限于深圳市赞悦科技有限公司内部传阅，禁止外泄以及用于其他的商业项目。
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="codedriver.module.cmdb.dao.mapper.ci.CiAuthMapper">
    <!--<cache type="codedriver.framework.dao.cache.CodeDriverCache" flushInterval="30000" size="100"></cache>-->

    <select id="getCiAuthByCiId" parameterType="java.lang.Long" resultType="codedriver.framework.cmdb.dto.ci.CiAuthVo"
            useCache="true">
        SELECT `ci_id`     AS ciId,
               `auth_type` AS authType,
               `action`,
               `auth_uuid` AS authUuid
        FROM `cmdb_ci_auth`
        WHERE ci_id = #{value}
    </select>

    <insert id="insertCiAuth" parameterType="codedriver.framework.cmdb.dto.ci.CiAuthVo">
        INSERT INTO `cmdb_ci_auth` (`ci_id`,
                                    `auth_type`,
                                    `action`,
                                    `auth_uuid`)
        VALUES (#{ciId},
                #{authType},
                #{action},
                #{authUuid})
    </insert>

    <delete id="deleteCiAuthByCiId" parameterType="java.lang.Long">
        DELETE
        FROM `cmdb_ci_auth`
        WHERE `ci_id` = #{ciId}
    </delete>

</mapper>
