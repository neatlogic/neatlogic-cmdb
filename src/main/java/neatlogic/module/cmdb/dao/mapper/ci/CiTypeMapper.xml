<?xml version="1.0" encoding="UTF-8" ?>


<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="neatlogic.module.cmdb.dao.mapper.ci.CiTypeMapper">
    <cache-ref namespace="neatlogic.module.cmdb.dao.mapper.ci.CiMapper"/>
    <select id="getMaxSort" resultType="java.lang.Integer">
        SELECT max(sort)
        FROM cmdb_citype
    </select>

    <select id="checkCiTypeNameIsExists" parameterType="neatlogic.framework.cmdb.dto.ci.CiTypeVo" resultType="int">
        SELECT count(1)
        FROM cmdb_citype
        WHERE name = #{name}
          AND id != #{id}
    </select>

    <select id="searchCiType" parameterType="neatlogic.framework.cmdb.dto.ci.CiTypeVo"
            resultType="neatlogic.framework.cmdb.dto.ci.CiTypeVo">
        SELECT
        `id`,
        `name`,
        `sort`,
        `is_menu` AS isMenu,
        `is_showintopo` AS isShowInTopo,
        `is_showincientityquery` AS isShowInCiEntityQuery,
        `icon`,
        (SELECT
        COUNT(1)
        FROM
        cmdb_ci ci WHERE ci.type_id = a.id) AS ciCount
        FROM
        `cmdb_citype` a
        <where>
            <if test="keyword != null and keyword != ''">
                AND name LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="isShowInTopo != null">
                AND `is_showintopo` = #{isShowInTopo}
            </if>
            <if test="isShowInCiEntityQuery != null">
                AND `is_showincientityquery` = #{isShowInCiEntityQuery}
            </if>
        </where>
        ORDER BY sort
    </select>

    <select id="getCiTypeListByIdList" parameterType="java.lang.Long"
            resultType="neatlogic.framework.cmdb.dto.ci.CiTypeVo">
        SELECT
        `id`,
        `name`,
        `sort`,
        `is_menu` AS isMenu,
        `is_showintopo` AS isShowInTopo,
        `is_showincientityquery` AS isShowInCiEntityQuery,
        `icon`
        FROM `cmdb_citype`
        WHERE `id` IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="getCiTypeByName" parameterType="java.lang.String"
            resultType="neatlogic.framework.cmdb.dto.ci.CiTypeVo">
        SELECT `id`,
               `name`,
               `sort`,
               `is_menu`       AS isMenu,
               `icon`,
               `is_showintopo` AS isShowInTopo,
               `is_showincientityquery` AS isShowInCiEntityQuery
        FROM `cmdb_citype`
        WHERE name = #{value}
    </select>

    <select id="getCiTypeById" parameterType="java.lang.Long" resultType="neatlogic.framework.cmdb.dto.ci.CiTypeVo">
        SELECT `id`,
               `name`,
               `sort`,
               `is_menu`       AS isMenu,
               `icon`,
               `is_showintopo` AS isShowInTopo,
               `is_showincientityquery` AS isShowInCiEntityQuery
        FROM `cmdb_citype`
        WHERE id = #{value}
    </select>

    <update id="updateCiType" parameterType="neatlogic.framework.cmdb.dto.ci.CiTypeVo">
        UPDATE
            `cmdb_citype`
        SET `name`          = #{name},
            `sort`          = #{sort},
            `is_menu`       = #{isMenu},
            `icon`          = #{icon},
            `is_showintopo` = #{isShowInTopo}
        <!-- 旧调用未提交新字段时保留原配置。 -->
        <if test="isShowInCiEntityQuery != null">
            , `is_showincientityquery` = #{isShowInCiEntityQuery}
        </if>
        WHERE `id` = #{id}
    </update>

    <insert id="insertCiType" parameterType="neatlogic.framework.cmdb.dto.ci.CiTypeVo">
        INSERT INTO `cmdb_citype` (`id`,
                                   `name`,
                                   `sort`,
                                   `is_menu`,
                                   `icon`,
                                   `is_showintopo`,
                                   `is_showincientityquery`)
        VALUES (#{id},
                #{name},
                #{sort},
                #{isMenu},
                #{icon},
                #{isShowInTopo},
                COALESCE(#{isShowInCiEntityQuery}, 1))
    </insert>

    <delete id="deleteCiTypeById" parameterType="java.lang.Long">
        delete
        from cmdb_citype
        where id = #{value}
    </delete>
</mapper>
