{
  "moduleId": "cmdb",
  "name": "cmdb_group_auth",
  "label": "cmdb_group_auth",
  "description": "Cmdb_group_auth is used for configuring related queries for configuration management and other reporting scenarios.",
  "fields": [
    {
      "name": "group_id",
      "type": "bigint",
      "description": "Retrieve the ID from the cmdb_group table, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "auth_type",
      "type": "enum('user','role','team','common')",
      "description": "User,role,team, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "auth_uuid",
      "type": "char(32)",
      "description": "Auth UUID, used as the primary key or unique identifier for records in this table."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "group_id",
        "auth_uuid",
        "auth_type"
      ],
      "description": "Primary key index covering the group_id, auth_uuid, auth_type fields, suitable for uniquely identifying records and queries that join the primary table."
    }
  ]
}
