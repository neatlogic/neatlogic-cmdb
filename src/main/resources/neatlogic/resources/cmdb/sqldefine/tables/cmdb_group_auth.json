{
  "moduleId": "cmdb",
  "name": "cmdb_group_auth",
  "label": "cmdb_group_auth",
  "description": "cmdb_group_auth 可用于配置管理相关的关联查询等报表查询场景。",
  "fields": [
    {
      "name": "group_id",
      "type": "bigint",
      "description": "引用cmdb_group表id，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "auth_type",
      "type": "enum('user','role','team','common')",
      "description": "user,role,team，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "auth_uuid",
      "type": "char(32)",
      "description": "auth uuid，作为当前表记录的主键或唯一定位字段。"
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "group_id",
        "auth_uuid",
        "auth_type"
      ],
      "description": "主键索引，覆盖 group_id、auth_uuid、auth_type 字段，适合用于唯一定位记录和主表关联查询。"
    }
  ]
}
