{
  "moduleId": "cmdb",
  "name": "cmdb_viewconst",
  "label": "cmdb_viewconst",
  "description": "Cmdb_viewconst is used for querying, filtering, and reporting on data related to configuration management.",
  "fields": [
    {
      "name": "id",
      "type": "bigint",
      "description": "ID, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "name",
      "type": "varchar(50)",
      "description": "Name."
    },
    {
      "name": "label",
      "type": "varchar(50)",
      "description": "Display name."
    },
    {
      "name": "sort",
      "type": "int",
      "description": "Sort."
    },
    {
      "name": "show_type",
      "type": "enum('none','all','list','detail')",
      "description": "The `show_type` field in the `cmdb_viewconst` table."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "id"
      ],
      "description": "Primary key index covering the ID field, suitable for uniquely identifying records and queries that join the primary table."
    }
  ]
}
