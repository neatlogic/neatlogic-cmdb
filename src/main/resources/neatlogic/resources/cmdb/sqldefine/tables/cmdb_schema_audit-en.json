{
  "moduleId": "cmdb",
  "name": "cmdb_schema_audit",
  "label": "cmdb_schema_audit",
  "description": "Cmdb_schema_audit is used for time dimension statistical queries and association queries related to configuration management, which are applicable to report scenarios.",
  "fields": [
    {
      "name": "id",
      "type": "bigint",
      "description": "ID, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "target_id",
      "type": "bigint",
      "description": "Target ID. A standard index is defined on this field, making it suitable for filtering, join, and sort queries."
    },
    {
      "name": "target_type",
      "type": "enum('ci','attr','rel','cientity')",
      "description": "Target type."
    },
    {
      "name": "action",
      "type": "enum('insert','delete','update')",
      "description": "Action."
    },
    {
      "name": "lcd",
      "type": "timestamp(3)",
      "description": "Update time. A standard index is defined on this field, making it suitable for filtering, join, and sort queries."
    },
    {
      "name": "server_id",
      "type": "int",
      "description": "Server ID. A standard index is defined on this field, making it suitable for filtering, join, and sort queries."
    },
    {
      "name": "data",
      "type": "text",
      "description": "Data."
    },
    {
      "name": "is_failed",
      "type": "tinyint(1)",
      "description": "Is it a failure."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "id"
      ],
      "description": "Primary key index covering the ID field, suitable for uniquely identifying records and queries that join the primary table."
    },
    {
      "name": "idx_lcd",
      "type": "BTREE",
      "unique": false,
      "primary": false,
      "columns": [
        "lcd"
      ],
      "description": "Standard index covering the lcd field, suitable for filtering, join, and sort queries."
    },
    {
      "name": "idx_server_id",
      "type": "BTREE",
      "unique": false,
      "primary": false,
      "columns": [
        "server_id"
      ],
      "description": "Standard index covering the server_id field, suitable for filtering, join, and sort queries."
    },
    {
      "name": "uk",
      "type": "BTREE",
      "unique": true,
      "primary": false,
      "columns": [
        "target_id",
        "target_type"
      ],
      "description": "Unique index covering the target_id, target_type fields, suitable for exact-match retrieval, uniqueness checks, and duplicate detection."
    }
  ]
}
