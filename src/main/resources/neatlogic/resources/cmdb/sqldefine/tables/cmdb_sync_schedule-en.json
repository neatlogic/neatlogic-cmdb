{
  "moduleId": "cmdb",
  "name": "cmdb_sync_schedule",
  "label": "CMDB Synchronization Schedule Table",
  "description": "The CMDB synchronization schedule is designed for use in association queries related to configuration management and other reporting scenarios.",
  "fields": [
    {
      "name": "id",
      "type": "bigint",
      "description": "ID, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "cron",
      "type": "varchar(500)",
      "description": "Cron expression."
    },
    {
      "name": "policy_id",
      "type": "bigint",
      "description": "Strategy_id. A standard index is defined on this field, making it suitable for filtering, join, and sort queries."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "id"
      ],
      "description": "Primary key index covering the ID field, suitable for uniquely identifying records and queries that join the primary table."
    },
    {
      "name": "idx_policy_id",
      "type": "BTREE",
      "unique": false,
      "primary": false,
      "columns": [
        "policy_id"
      ],
      "description": "Standard index covering the policy_id field, suitable for filtering, join, and sort queries."
    }
  ]
}
