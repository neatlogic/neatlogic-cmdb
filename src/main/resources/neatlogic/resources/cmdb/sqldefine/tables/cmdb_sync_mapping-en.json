{
  "moduleId": "cmdb",
  "name": "cmdb_sync_mapping",
  "label": "cmdb_sync_mapping",
  "description": "The cmdb_sync_mapping can be used for configuration management-related queries and reporting scenarios.",
  "fields": [
    {
      "name": "id",
      "type": "bigint",
      "description": "ID, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "ci_collection_id",
      "type": "bigint",
      "description": "Model_set_id."
    },
    {
      "name": "rel_id",
      "type": "bigint",
      "description": "Relationship ID."
    },
    {
      "name": "direction",
      "type": "enum('from','to')",
      "description": "Direction."
    },
    {
      "name": "global_attr_id",
      "type": "bigint",
      "description": "Global attribute ID."
    },
    {
      "name": "attr_id",
      "type": "bigint",
      "description": "The attribute ID."
    },
    {
      "name": "field",
      "type": "varchar(100)",
      "description": "Collection fields, support JSONPath."
    },
    {
      "name": "action",
      "type": "enum('insert','replace','delete','update','merge')",
      "description": "Action."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "id"
      ],
      "description": "Primary key index covering the ID field, suitable for uniquely identifying records and queries that join the primary table."
    }
  ]
}
